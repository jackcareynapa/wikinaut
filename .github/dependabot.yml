# Docs: https://docs.github.com/en/code-security/dependabot/working-with-dependabot/dependabot-options-reference

version: 2
updates:
  - package-ecosystem: 'pip'
    directory: '/'
    schedule:
      interval: 'quarterly'
    cooldown:
      default-days: 2
    groups:
      minor-and-patch:
        patterns: ['*']
        update-types: ['minor', 'patch']

  # Keep the CI workflow's action pins current; without this they silently rot.
  - package-ecosystem: 'github-actions'
    directory: '/'
    schedule:
      interval: 'quarterly'
    cooldown:
      default-days: 2
    groups:
      actions:
        patterns: ['*']
